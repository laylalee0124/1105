package inheritance_learn;


//////////////////////////////////////상속의 개념////////////////////////////////////////
/*
1) 똑같은 명령을 내려도, 처리는 다르게 되도록 짜보기.
2) 메인 명령 / 서브 명령.
 - 'service' 처리 명령.
 - 'service-1' = create, 'service-2' = remove 등등 여러가지 기능이있음.
 - if-else 구문에 따라 sevice의 실행 형태가 달라짐.
3) 명령어를 일원화 시키면 어떤 값이 입력되냐에 따라 하나의 명령어로 다양하게 실행가능.
   = 수정이 필요하더라도 일일히 소스를 고치지않아도 되어서 좋음. 

*/

//-----부모 클래스------

//-----부모를 상속하는 자식 클래스------

//------자식을 상속하는 손자 클래스----------------


//----출력 테스트----
	public class Inheritance07 {
		public static void main(String[] args) {
			/* ----------예제--------------
			Animal anml=null;  // 우선 null값으로 setting해놓고 if-else구문을 통해 아래와 같이 결과값을 setting할수도있음.
			if(1==1) {
				anml=new Mammal();
			}else{
				anml=new Reptile();
			}
			anml.run();
			*/ 
			
		}
	}